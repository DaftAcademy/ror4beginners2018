= simple_form_for @track do |f|
 = f.input :title, label: 'Title of the track'
 - if @artist
  = f.hidden_field :artist_id, value: @artist.id
 - else
  = f.input :artist_id, collection: @artists
 = f.button :submit, class: 'btn btn-primary'