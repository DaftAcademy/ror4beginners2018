%div.container
  %h2 #{@playlist.title} playlist
  %table.table
    %thead
      %tr
        %th Title
        %th Artist
    - @playlist.tracks.each do |track|
      %tr
        %td #{track.title}
        %td #{track.artist.name}